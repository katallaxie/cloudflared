# Cloudflared Docker Image for ARMv5 Devices (e.g., MikroTik hEX refresh)

The image is build only via GitHub Action. It is triggered manually.

## Usage

```bash
docker run -d --name katallaxie/cloudflared -e TUNNEL_TOKEN=""
```

## License

![License](/LICENSE)
